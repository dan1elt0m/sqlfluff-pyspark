repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.13.0
    hooks:
      - id: ruff-format
        args: [ --config=pyproject.toml ]
      - id: ruff-check
        args: [ --config=pyproject.toml ]